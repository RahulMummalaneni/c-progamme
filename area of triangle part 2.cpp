#include<stdio.h>
main()
{
	float b,h,area;
	printf("the height of the triangle :");
	scanf("%f",&h);
	printf("the base of the triangle :");
	scanf("%f",&b);
	area=b*h/2;
	printf("the area of triangle:%f",area);
	}
